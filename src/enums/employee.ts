export namespace EMPLOYEE {
  export namespace TYPE {
    export enum OTHER {
      Regular = 'Regular',
      Contingent = 'Contingent Worker',
    }
    export enum STATUS {
      EMPLOYED = 'EMPLOYED',
      LEFT = 'LEFT',
      PROBATION = 'PROBATION',
    }
  }
  export enum STATUS {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    DELETED = 'DELETED',
  }
}
